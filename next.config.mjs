/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        remotePatterns: [
            { hostname: "strong-sandpiper-465.convex.cloud" }
        ],
    },
};

export default nextConfig;
